# Assets use public and project scopes only

BioMap Agent is a ToB R&D platform, so asset access follows organization and project boundaries rather than personal drive ownership. The Assets Workbench supports public scope and project scope only; users who need a personal working area should create a Project that represents that area. This avoids adding a third personal permission model that would conflict with project-based collaboration, agent context sharing, and later backend authorization.
