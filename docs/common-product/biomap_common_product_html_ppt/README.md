# BioMap 通用产品 HTML PPT

首版基于同目录上级的 `biomap_common_product_project_proposal.md` 制作。

## 文件

- `index.html`: HTML 型演示稿，可直接用浏览器打开。
- `source-notes.md`: 来源、叙事提炼和素材约束。
- `profile-plan.md`: deck profile、证明对象和 QA 约束。

## 使用

直接打开 `index.html`。左右方向键翻页；浏览器打印时可保存为 PDF。
